package com.quizGpt.formManagement.Quiz.Controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class QuizController {

    /*
    create Quiz 
    ResponseEntity<QuizDto> generateQuiz(@RequestBody request) {
        this is where we would make call to microservice that generates quiz. Do this by sending message to queue for the service,

    }

    view all quizes 

    view a specific quiz/id

    delete a specific quiz 

    delete all quizes 

    will need a RabbitMQMService to help send messages to the queue, as all as implement a listener function that will save the 
    quiz to a table in the database 
    */
    
}
